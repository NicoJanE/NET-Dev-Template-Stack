// template_launch.webapi.json
{
  "version": "0.2.0",
  "configurations": [

    // ---------------------------------------------------
    // Docker Debug Launcher (Web API)
    // Use when VS Code is ATTACHED to the running container
    // ---------------------------------------------------
    {
      "name": "Launch Web API in Docker",
      "type": "coreclr",
      "request": "launch",
      "preLaunchTask": "build (Docker)",
      "program": "dotnet",
      "args": [
        "/hostmount/workspace/your-app/bin/Debug/net8.0/your-app.dll"       // 1. UPDATE the Directory and .dll name
      ],
      "cwd": "${workspaceFolder}",
      "stopAtEntry": false,

      "env": {
        "ASPNETCORE_URLS": "http://0.0.0.0:5000",
        "ASPNETCORE_ENVIRONMENT": "Development"
      },

      "serverReadyAction": {                                              // Can be used for Swagger
        "action": "openExternally",
        "pattern": "\\bNow listening on:\\s+https?://[^:]+:(\\d+)",
        "uriFormat": "http://localhost:%s/swagger"
      },

      "console": "internalConsole"
    },

    // ---------------------------------------------------
    // Windows Local Debug Launcher (Web API)
    // ---------------------------------------------------
    {
      "name": ".NET Core Launch (Windows Web API)",
      "type": "coreclr",
      "request": "launch",
      "preLaunchTask": "build (Windows)",
      "program": "${workspaceFolder}/bin/Debug/net8.0/your-app.dll",   // 1. UPDATE the .dll name
      "args": [],
      "cwd": "${workspaceFolder}",
      "console": "internalConsole",
      "stopAtEntry": false,

      "env": {
        "ASPNETCORE_URLS": "http://localhost:5000",
        "ASPNETCORE_ENVIRONMENT": "Development"
      },

      "serverReadyAction": {
        "action": "openExternally",
        "pattern": "\\bNow listening on:\\s+https?://[^:]+:(\\d+)",
        "uriFormat": "http://localhost:%s/swagger"
      }
    },

    // ---------------------------------------------------
    // Generic Attach. 
    //    Debug an already running application (Docker / Windows / VM)
    // ---------------------------------------------------
    {
      "name": ".NET Core Attach",
      "type": "coreclr",
      "request": "attach"
    }
  ]
}